rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open read/write access for standalone software, web, and mobile app sync
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
